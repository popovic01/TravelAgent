namespace TravelAgent.Utility
{
    public class StripeSettings
    {
        //same properties as in appsettings.json
        public string SecretKey { get; set; }
        public string PublishableKey { get; set; }
    }
}
